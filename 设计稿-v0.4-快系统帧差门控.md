# 设计稿 v0.4 · 快慢系统分层（借鉴 VUS 已论证经验）——帧差门控 + 预算分层

> 状态：**待审阅**（前置：v0.2 发现直连 / v0.3.1 环境识别与网格圈选）
> 日期：2026-09-17
> 定位：NVR 的**预算闸门**——检测、告警、理解等一切"贵的动作"只在有动的时刻发生。
> 本稿只定义行为与经验来源；代码级实现到开发阶段另行确认。

---

## 1. 为什么 NVR 需要帧差门控（VUS 已论证）

VUS 的核心论点在本仓同样成立：**贵的推理只为值得的时刻发生**。实测依据（vus 基准）：

| VUS 实测项 | 数据 | 对 NVR 的意义 |
|---|---|---|
| 帧差门控单帧成本 | ~1.6ms/帧（降采样灰度帧差） | 每相机常驻逐帧门控，CPU 占用可忽略 |
| 全帧 vs 门控的分析负载差 | 45 分钟 81,878 帧逐帧全分析 vs 门控后仅 ~3% 帧触发重活 | 检测/嵌入/VLM 调用量降一个数量级以上 |
| 双码流实践 | 画面链 5.9× 实时（147.7fps 处理速率） | NVR 多相机并行门控余量充足 |
| 事件零丢失 | 45 分钟压测 0/59,556 事件丢弃 | 门控不漏事 |

---

## 2. 在 NVR 中的位置：双流架构

```
相机 ──┬─ 主码流 → ffmpeg 流拷贝 → 录像分段 mp4（NVR 本职，画质优先）
       └─ 子码流 → vus FrameSource 解码 → 帧差门控（本稿）→ 语义层
```

- **主码流录制**与**子码流分析**并行，互不抢占（标准 NVR 双流实践；摄像头普遍支持 ≥2 并发连接）；
- 门控在**子码流解码帧**上运行：降采样灰度 → 帧差 → 阈值+面积双闸 → 运动事件（含 motion_ratio、运动区域）；
- 语义层一切"贵动作"由运动事件触发。

---

## 3. 门控规格（参数默认值全部取自 VUS 实测口径）

| 项 | 默认 | 来源/说明 |
|---|---|---|
| 预处理 | 降采样灰度（宽 ~96px，帧内复用画布） | vus T0 口径，1.6ms/帧的成本构成 |
| 像素差阈值 | 25（0-255 灰度） | vus 默认；边界实测：恰为 25 不触发、26 触发 |
| 触发面积门槛 | 变化像素数门槛（按分辨率比例） | vus：单像素噪声不触发、16 像素块越过面积门槛触发 |
| 输出 | `{motion_ratio, 运动框}` | motion_ratio 逐帧采集——漂移监控的输入 |
| 静止行为 | 不触发重活，记录 ratio | "有没有事"的答案绝大多数帧是"没有" |

---

## 4. 门控与网格圈选的联动（本项目的独有落点）

运动事件 → **映射到管理员圈定的网格**：

- 运动框覆盖的管理格 ≥1 → **重点运动**：优先送检测/告警判定（预算内最高优先）；
- 运动仅在未圈格 → **背景运动**：不计告警、不占检测预算，仅入统计；
- 管理员"圈了格但整天没动静"与"没圈格但全是动静"都能从 stats 里看清——**告警预算跟着管理员关注的格子走**。

---

## 5. VUS 两条血泪经验的随迁（防同类坑）

1. **渐变漂移双车道**：只设"超阈值触发"会漏掉缓慢变化（相机缓移/光照渐变/镜头缓推）——
   硬车道（超阈值在滑窗内累计达 N 次）之外，保留**软车道**（低幅变化持续占满时间窗也确认），
   采纳即重置基准。此经验来自 vus 慢系统漂移 bug 的两轮修复；
2. **场景切换软重置**：成像突变（ICR 昼夜切换、镜头转动、灯光开关）必然引发帧差爆炸——
   检测到整帧级突变时**重置背景基准并抑制该次假触发**，而非让全系统跟着"看到全世界都在动"。
   （语义层 ICR 状态机的第一输入即来自这里。）

---

## 6. 交给上层的事件与预算语义

| 门控产出 | 去向 | 预算语义 |
|---|---|---|
| 重点运动（管理格内） | 检测器触发（NanoDet，仅此时跑） | 检测预算 = 运动频度的函数，非帧率函数 |
| 背景运动 | 仅统计 | 零成本 |
| 持续运动窗（如 10s 连续） | 段关闭 → V-JEPA 嵌入比对 → 未命中才 VLM | 理解预算 = 新异率驱动（习惯化后趋零） |
| 整帧级突变 | 背景软重置 + ICR 候选 | 零成本 |

---

## 7. 快慢系统全景（VUS 思想的完整落地）

v0.3 节的帧差门控只是快系统。完整的分层栈——每层回答自己的问题、有自己的触发与预算、滞后有界：

| 层 | 职责（回答的问题） | 触发 | 单次成本 | 滞后界 |
|---|---|---|---|---|
| **T0 门控**（快系统，常驻） | 有没有事发生？ | 逐帧 | ~1.6ms | 0（同帧） |
| **T1 检测** | 谁、在哪、什么类别？ | 运动触发（400ms/巡检 5s） | 23-30ms | ≤2.5s（P95） |
| **裁决** | 该不该报警？ | T1 确认即裁决（管理员规则） | <10ms | ≤1s——**报警路径零慢层**：全程不经 JEPA/VLM |
| **T2a 嵌入比对**（慢系统·快车道） | 是不是已知模式？ | 段关闭即嵌（V-JEPA ~40ms GPU） | ≤2s | ≤5s |
| **T2b VLM 深理解**（慢系统·深车道） | 新事件意味着什么？ | **仅 T2a 未命中**（新异事件）/ 管理员按钮 | ~6s（qwen3-vl 实测） | ≤30s（SLA）+ min_call_interval 地板 |
| **T3 沉淀** | 哪些模式在重复？（习惯化/档案） | 段关闭后批量 | 段频 | 小时级可接受 |

**每层失效不拖垮其他层**：T2 挂了 → 模板占位名顶上，T0/T1 照常值守（降级链）；T1 挂了 → 门控照跑、告警降级为运动记录。快系统永不等待慢系统。

## 8. 快慢纪律（五条，贯穿所有层）

1. **快系统永不等慢系统**：有界订阅、慢消费者丢最旧（vus EventBus 机制）——帧循环 1.6ms 的预算绝不被慢层拖住；
2. **慢系统素材只累积不排队**：单飞合并——VLM 忙时新证据并入下一窗，不排队不丢单（vus.live 单飞语义）；
3. **滞时有界不随时间增长**：慢层最坏滞后 = 一次调用时长 + 地板间隔，**运行一年和运行一天一样快**；
4. **成本可预算**：每层成本 = 速率 × 单次成本，总账逐层封顶（检测频度由门控决定、VLM 由地板间隔决定、习惯化使调用量衰减）；
5. **分层作答**：低层在慢层处理期间照常产出（检出/裁决/录像不停），慢层结果事后**增补**（命名 r2 覆写 r1）而非阻塞；
6. **报警路径零慢层**：告警 = T1 检测 + 管理员规则裁决，全程不碰 JEPA/VLM——报警速度就是检测速度，无任何语义层前置；
7. **慢系统 JEPA 优先**：能比对档案解决的绝不开 VLM——已知模式毫秒级自命名，**VLM 只接新异事件**（新异=嵌入比对未命中），VLM 从"常规处理层"降为"新异仲裁层"；
8. **事件上下文即用即归档**：每事件独立有界上下文（环境档案 + 本段摘要 + ≤3 帧），段关闭即归档 SQLite、热区清零——**事件永不堆积在同一上下文空间**；
9. **图片按需递增**：VLM 调用默认只给 1 帧（峰值帧），首轮 undecidable 才递增至 2/3 帧——不无条件读取抽取图片，token 与单帧证据挂钩。

## 8.1 慢系统车道分工（JEPA 主判，VLM 深判）

**监控事件的文本口径（与 VUS 连贯视频的本质差异）**：监控事件往往**无法用一句话简单形容**——真实定义需要细节语言（人物外观特征/所处位置/动作序列/方向/持续时间/先后顺序）。因此事件产出三层文本：

| 层 | 内容 | 用途 |
|---|---|---|
| **短名** | ≤16 字中文名（"可疑人员翻墙入院"） | 事件列表与通知标题 |
| **细节描述** | 细节化自然语言，**不设字数上限**："深夜一名穿深色连帽衫的人员自围墙东侧翻入，在电表箱前停留约三分钟，多次张望后原路返回" | 事件档案、检索、审查凭据 |
| **rationale** | ≤30 字判定理由 | 审计 |

细节描述**随模式归档**：某模式的首次 VLM 细节描述存入模式档案，此后习惯化命中直接复用（细节也可随后续观测增补）——**重复事件零 VLM 且不丢细节**。

| 情形 | 走法 | 速度 |
|---|---|---|
| 段嵌入命中已知模式 | 直接采用档案名 + 档案细节描述（自命名），零 VLM | 毫秒级 |
| 段嵌入未命中（新异） | VLM 命名 + 细节描述 + 建新档 | ≤30s，随新异率衰减 |
| 灰区行为判定 | JEPA 距离三分类：已知安全/已知危险/**未知** | 毫秒级；未知才送 VLM |
| 管理员改名/误报标记 | 直接更新档案（金标），无需模型 | 即时 |

**习惯化画面学习（简单闭环，节约 token 的主手段）**：嵌入命中 → 自命名 → 低频抽检（5%），不做更重的治理机关——目标只有一个：**重复场景的 VLM 调用趋零**。

### 8.2 用户自定义模板（报警=识别结果对比模板符合即报）

模板 = 管理员自定义的"什么情况要报警"，**完全开放自定义**（内置三模板仅为预设示例），每条两部分：

| 组成 | 层 | 报警速度 |
|---|---|---|
| **结构条件**：网格区域 + 类别 + 时间窗 + 滞留秒数 | 快系统实时比对（T1 检测+规则） | **≤1s 实时报警** |
| **语义描述**（可选）：自然语言细节（"有人翻墙进入而不是走大门"） | 慢系统识别的细节描述与模板语义比对（T2b） | ≤30s 补充报警 |

快慢系统识别结果持续与模板库比对：**符合结构条件 → 实时报警**；细节描述与语义模板相符（结构条件抓不住的情形）→ 补充报警。模板库随场所档案持久化，工作台可视化增删改。

## 9. 完整事件旅程（一次夜间入侵，穿五层）

1. T0：门控在管理格内发现运动（1.6ms）→ 触发检测；
2. T1：400ms 内 NanoDet 检出 person → 确认 → 落入管理员圈定的区域、滞留计时；
3. 裁决：滞留达标 → **alert，≤1s 出告警**（横幅+事件记录+webhook）——不等命名；
4. 段关闭 → T2a：V-JEPA 嵌入比对档案 → 命中已知模式则毫秒级自命名（短名+档案细节描述，零 VLM）；本例为新异 → T2b：VLM 命名 + **细节描述**（1 帧起步，undecidable 才递增帧数）→ "可疑人员翻墙入院" + 细节："凌晨一名穿深色连帽衫的人员自围墙东侧翻入，在电表箱前停留约三分钟后原路返回"（≤30s）→ 命名 r2 覆写 + 新档建立（细节描述随档）；
5. T3：V-JEPA 段嵌入入档；同签名再现 → 习惯化直接自命名，零 T2 调用。

## 10. 待拍板点

1. **子码流分辨率档位**：建议 640×360 档（门控与检测够用、解码省）；主码流仅进录像；
2. **运动框输出粒度**：一期单个合并框（够用）；二期多框/分格热力；
3. **软车道参数**：滑窗长度与占满比沿用 vus 默认，部署后按场所校准；
4. **多相机门控预算**：每相机独立门控线程（vus 帧循环模型），CPU 按路数线性——8 路以内无压力（实测单路门控 ≪1% CPU 量级）。

# 设计稿 v0.2 · 自动发现 + 直连 RTSP（取代 v0.1）

> 状态：**待审阅**（v0.1 的多路启动器部分不变，本稿新增【自动发现与直连】作为 v0.1 前置）
> 日期：2026-09-17
> 原则升级：**用户不做任何相机配置——插网线、跑一条命令、出画面**

---

## 0. 用户要的体验（目标交互）

```
$ scam discover
[发现] ONVIF 组播探测中（3 秒）…
[发现] 找到 2 台设备：
  ┌──┬────────────────┬──────────────┬─────────────────────────┐
  │ # │ IP             │ 厂商          │ RTSP 流                 │
  ├──┼────────────────┼──────────────┼─────────────────────────┤
  │ 1 │ 192.168.1.64   │ Hikvision    │ ✅ 已验证可解码（子码流） │
  │ 2 │ 192.168.1.108  │ Dahua        │ ⚠️ 需凭证（--password）  │
  └──┴────────────────┴──────────────┴─────────────────────────┘
选择接入（1 / 2 / all）: 1
[接入] vus.live 已启动 → rtsp://192.168.1.64:554/Streaming/Channels/102
       理解结果: live_state.json + SSE :8600/events
```

一条命令，从"不知道家里有啥摄像头"到"vus 正在理解这台摄像头"。

---

## 1. 发现机制（三层递进，全自动）

| 层 | 手段 | 覆盖 | 依赖 |
|---|---|---|---|
| L1 **ONVIF 组播发现** | WS-Discovery probe → UDP 239.255.255.250:3702，3 秒收响应 | 海康/大华/Axis/宇视等主流品牌（ONVIF 开启即响应，多数出厂默认开） | Python `wsdiscovery` 包（纯 Python） |
| L2 **RTSP 端口探测** | 对 `--range` 网段（默认 /24）探 554（RTSP）/ 8000（海康 SDK）端口 | ONVIF 关闭但 RTSP 开的设备 | 标准socket，无依赖 |
| L3 **取流验证** | 对每台候选：ONVIF GetStreamUri（带凭证）→ 品牌模式穷举 → `ffprobe` 3 秒验证可解码 | 最终确认"真的能出画面" | ffprobe（vus 已依赖） |

**品牌 RTSP 模式表**（L3 穷举用，命中即停）：
- Hikvision：`rtsp://u:p@ip:554/Streaming/Channels/101`（主）/`102`（子）
- Dahua：`rtsp://u:p@ip:554/cam/realmonitor?channel=1&subtype=0`
- 通用 ONVIF：GetStreamUri 返回的地址
- 兜底：`rtsp://u:p@ip:554/live`、`/h264`、`/1`

## 2. 凭证与现实的诚实边界

1. **取流地址分两档**：ONVIF GetStreamUri 需要设备凭证；**探测/发现本身不需要**——所以无凭证也能"发现设备列表"，给凭证才能"验证流可播"；
2. **海康新机激活墙**：未激活的出厂相机所有协议锁死（这是海康安全策略，任何工具都绕不开）——首次需用 SADP/网页设一次密码，之后永久免配置。发现工具会明确提示"已发现未激活设备，请先激活"而不是沉默失败；
3. **同一局域网**：组播不过路由器——运行本工具的机器必须与相机同网段（跨网段/公网场景退回直接填 URL，v0.1 已支持）；
4. **凭证只问一次**：家庭场景通常同品牌同密码，`--user admin --password xxx` 一组凭证打全场；验证成功的写入 cameras.json，之后启动不再询问。

## 3. 命令面（scam 启动器，v0.1 的 ~150 行 + 发现 ~200 行）

```bash
scam discover                                   # 无凭证：只发现设备列表
scam discover --user admin --password xxx       # 带凭证：发现+取流+验证
scam discover --range 192.168.1.0/24            # 追加端口探测（覆盖 ONVIF 关闭设备）
scam start                                      # 按 cameras.json 拉起 vus.live（v0.1 已设计）
```

依赖增量：`wsdiscovery`（纯 Python）。发现/验证全部复用 vus 已有的 ffprobe。

## 4. 交付计划

| 阶段 | 内容 | 验收 |
|---|---|---|
| M0 | L1 组播发现 + 设备表输出 | 在真实局域网列出摄像头（IP/厂商） |
| M1 | L3 取流验证 + 品牌模式穷举 + cameras.json 自动生成 | 凭证正确时自动验证流并直接 `scam start` 出画面 |
| M2 | L2 端口探测 + 未激活设备识别提示 | ONVIF 关闭设备也能被发现并给出引导 |

## 5. 与 v0.1 的关系

v0.1 的启动器/多进程守护/零安装原则**全部不变**；本稿只是在它前面加"发现与直连"这个门。vus.live 本身仍零改动（消费的就是一个 RTSP URL）。

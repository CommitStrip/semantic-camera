# 实施计划 · 语义摄像头 v2（NVR 语义值守系统）

> 状态：**待拍板**（批准后按 P0→P5 顺序执行，每阶段独立验收）
> 日期：2026-09-17
> 目标锚：快 ≤1s / 准 金标反馈 / 省调用趋零 / 稳 7×24 / 私数据不出 NVR
> 技术底座：Python 3.10+ 单进程应用，唯一上游 vus（本地路径依赖）

---

## 0. 项目形态与目录

```
语义摄像头/
  pyproject.toml            # 包 scam；依赖：vus(本地路径)、numpy、opencv-python、
                            # onnxruntime、pillow、wsdiscovery
  scam/
    __init__.py
    config.py               # 场所档案（venue+camera 两层）加载与 fail-closed 校验
    db.py                   # SQLite：events/segments/patterns/embeddings/meta
    source.py               # 相机源接入（薄封装 vus FrameSource + 断流守护）
    gate.py                 # T0 门控（封装 vus SmartPipeline 口径：阈 25/面积双闸）
    detect.py               # T1 NanoDet onnxruntime（numpy 解码移植）
    track.py                # 跟踪确认（恒速预测+分级老化+类别守卫）
    zones.py                # 网格区域（22×18 选中格集合）+ 模板结构条件
    verdict.py              # 四态裁决 + 报警执行器（≤1s 路径，零慢层）
    segments.py             # 事件分段 + 行为签名（含 dwellMaxMs）
    embed.py                # T2a V-JEPA（预处理按 parity 结论：292 BILINEAR→裁256²）
    patterns.py             # PatternLibrary（信任分层/审计/双嵌入档案/联合匹配）
    naming.py               # T2b VLM 命名（三层文本：短名/细节描述/rationale）
    templates.py            # 自定义模板库（结构条件+可选语义描述）
    outbox.py               # 出口（webhook；at-least-once+死信）
    models.py               # Provider 抽象：local ollama / cloud openai-compat
    server.py               # 工作台 HTTP + SSE + 静态页服务
    discover.py             # v0.2 三层发现（ONVIF/端口/品牌模式/ffprobe）
    web/                    # 工作台静态页（vanilla JS + SSE，零构建链）
  tests/                    # pytest（全部逻辑层）
  cameras.json              # 相机接入（discover 生成或手填）
  venue.json                # 场所档案（环境+区域+模板+规则）
```

**运行入口**：`scam discover`（发现直连）→ `scam start`（值守：N 进程监控 + 工作台 :8600）→ 浏览器 `http://<NVR>:8600`。

---

## 1. 阶段计划（P0→P5，每阶段独立验收）

### P0 骨架与地基（0.5 天）
- 内容：包骨架、场所档案 schema+校验、SQLite 建表、pytest 骨架、vus 本地依赖接通
- 验收：config 合法/非法用例绿；`import scam` 在干净环境可跑

### P1 快系统端到端（1.5 天）→ 验收线【快】
- 内容：源接入 → T0 门控（vus 口径）→ T1 NanoDet → 跟踪 → 网格区域+模板结构条件 → 四态裁决 → 告警执行器（横幅/events.jsonl/webhook）
- 验收：**测试视频/真实流上，结构条件触发 → 告警 ≤1s**；检测仅运动触发（门控生效证据：检测调用次数 ≪ 帧数）；误报有 events 记录可回放

### P2 慢系统双车道（1.5 天）→ 验收线【省】
- 内容：分段+签名 → V-JEPA 嵌入（预处理按 parity 结论）→ PatternLibrary（含双档案/联合匹配）→ T2a 自命名 → T2b VLM（1 帧起步递增、细节描述不设上限、环境档案上下文）→ 模板语义比对
- 验收：同签名第二段 **零 VLM 自命名**（毫秒级）；新异事件 ≤30s 出三层文本；VLM 调用计数随重复段下降（输出调用统计）

### P3 工作台（1.5 天）→ 验收线【准】
- 内容：HTTP+SSE 服务、六视图静态页（值守/磨砂门/圈选/事件/模式库/系统）、金标反馈（误报标记+改名→档案即时更新）、模型双通道配置页+连通性测试
- 验收：浏览器完成全流程（连接→识别→圈选→收告警→标记误报→金标改名生效）；误报标记即时改变告警行为

### P4 发现与直连（1 天）
- 内容：L1 ONVIF 组播（wsdiscovery）→ L2 网段端口探测 → L3 品牌模式+ffprobe 验证 → cameras.json 自动生成
- 验收：真实局域网列出摄像头；凭证正确时自动验证流并直连出画面

### P5 稳态与验收（1 天）→ 验收线【稳】【私】
- 内容：24h 值守浸泡（内存曲线/断流恢复/保留策略）、验收线逐项实测报告、安装文档（两条命令）
- 验收：**24h 内存有界平台期；断电断网恢复后自动回值**；私有性审计（核对唯一出 NVR 通道=可选裁剪帧，且默认关）

---

## 2. 关键技术决策（实现期已定，不再开放）

| 决策 | 选择 | 来源 |
|---|---|---|
| 感知 | vus FrameSource + SmartPipeline 门控口径（阈 25/面积双闸/1.6ms） | vus 实测 |
| 检测 | NanoDet ONNX onnxruntime，触发式（运动 400ms/巡检 5s） | 网关脚本 numpy 解码已验证 |
| 嵌入 | V-JEPA ONNX + PIL 复刻预处理（parity 1.0 结论平移） | 本机已实证 |
| VLM | ollama 进程内调用（num_ctx/num_predict 硬保障，1 帧起步递增） | 实测结论平移 |
| 存储 | SQLite（参数绑定 SQL） | 零部署 |
| UI | NVR 自带静态页 vanilla JS + SSE，桌面优先 | 零前端工程 |
| 密钥 | 仅环境变量/密钥服务；全仓零凭据字面量 | 红线 |

---

## 3. 与目标的映射（每条验收线的证据来源）

| 验收线 | 证据产出（P5 交付） |
|---|---|
| 快 ≤1s | 告警路径打点（T1 确认→告警出）实测分布报告 |
| 准 | 误报标记→行为变化演示 + 事件可回放 |
| 省 | VLM 调用计数器：重复场景调用趋零曲线 |
| 稳 | 24h 浸泡报告（内存曲线/断流恢复记录） |
| 私 | 代码审计清单：全仓唯一出 NVR 通道=可选裁剪帧（默认关） |

---

## 4. 依赖与风险

| 项 | 处置 |
|---|---|
| vus 依赖 | 本地路径可编辑安装（`pip install -e <vus路径>`）；frame_hook 上游增量（+3 行）待与 vus 主线协调，未合入前用选项 C（封装）过渡 |
| NVR 硬件未知 | 检测触发式+嵌入段频，CPU 量级可承受；P5 实测确认 |
| V-JEPA 权重分发 | 导出脚本随仓，NVR 上执行导出（或拷贝 onnx） |
| ollama 未装 | 慢系统降级：模板占位名 + 嵌入比对照常（快路径零影响） |
| 旧仓 | 封存不推送、不依赖；遗留问题（密码/历史）与本项目解耦 |

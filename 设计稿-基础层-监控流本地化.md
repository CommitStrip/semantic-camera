# 基础层设计稿 · 监控流本地化（语义摄像头的地基）

> 状态：**待审阅**（决策点已给默认值，批准即开工；预计 2-3 个工作日到 M2）
> 日期：2026-09-17
> 上游：仅 VUS（其 RTSPFrameSource/管线直接消费本层产物）
> 序位：**先于**语义摄像头 v2（语义设计稿-v2-重铸.md 暂缓，其开放问题 #1"录像凭据"由本层回答）

---

## 0. 目标一句话

**把监控流 7×24 抓到本地磁盘，变成有序、有健康监控、有保留策略的录像档案，让 vus 全家桶（离线管线 / vus.live / 未来的语义层）能像消费普通视频文件一样消费它。**

不做的事（本层刻意不碰）：语义、检测、告警——那是上层的事。本层只做"可靠的录像基建"。

---

## 1. 三条设计决定（默认值，可否决）

| # | 决定 | 默认值 | 理由 |
|---|---|---|---|
| D1 | 录制引擎用 **ffmpeg 子进程**（`-c copy` 流拷贝 + segment 分段），不自研封装 | 子码流连续录像 | 流拷贝零 CPU；ffmpeg 断线重连参数成熟；OpenCV 写 mp4 脆弱是业界共识 |
| D2 | **分段文件**而非单一大文件：每段 10 分钟，按 `相机/日期/` 目录落盘 | `storage/cameras/<id>/2026-09-17/seg_20260917-143000.mp4` | 分段文件=业务消费的最小单元（vus 管线天然吃文件）；删旧=删目录，零碎片 |
| D3 | **保留策略双闸**：按天数（默认 7 天）+ 按容量水位（默认磁盘 80%）双触发，删最旧 | 每日清理任务 + 每小时水位检查 | 防磁盘写满比防过期更重要 |

---

## 2. 架构（单机单进程 + 每相机一个 ffmpeg 子进程）

```
海康 RTSP ──┐
手机推流 ───┤── capture（Python 常驻进程）
网关 HLS ───┘      ├─ 每相机一个 ffmpeg：rtsp/HLS → segment mp4（-c copy）
                   ├─ 看门狗：进程退出/无增长 → 指数退避重启 + 事件记录
                   ├─ 保留策略：每日按龄清理 + 每小时容量水位
                   └─ index.jsonl（清单）+ stats.json（健康）
   ▼
storage/cameras/<id>/日期/seg_*.mp4     ← 业务消费的唯一接口（普通文件）
```

**业务怎么消费**（"为业务服务"的落地）：
- **离线分析**：`vus.integrated_pipeline --video storage/.../seg_xxx.mp4`——vus 管线原样吃文件，产出关键帧/代表帧/理解报告；
- **在线理解**：`vus.live --source rtsp ...` 与录制并行跑同一路流（各拉各的，互不干扰）；
- **回溯处理**：任何一段历史录像随时可送 vus 复盘（"昨晚的录像过一遍语义分析"= 一条命令）；
- **后续语义层**：语义摄像头 v2（暂缓）将来直接挂在本层之上，录制是它的"录像凭据"地基。

---

## 3. 相机接入配置（cameras.json，唯一手工维护点）

```json
{
  "cameras": [
    {
      "id": "front-door",
      "enabled": true,
      "source": "rtsp://user:pass@192.168.1.64:554/Streaming/Channels/102",
      "mode": "copy",              // copy=流拷贝(零CPU,要求H.264) | transcode=重编码(兜底)
      "segment_minutes": 10,
      "retention_days": 7,
      "storage_cap_gb": null       // null=只按天清理；设数值=同时按容量水位
    }
  ],
  "storage": { "root": "storage/cameras", "watermark_pct": 80 }
}
```

支持源类型：RTSP（海康主/子码流）、HTTP-FLV/HLS（网关路径，走 ffmpeg 一样拉）。家庭网关的 `cam` 路径（WHEP/HLS）也可接：HLS 用 ffmpeg `-i http://.../index.m3u8` 即可。

---

## 4. 健康与可观测（录像基建的命门是"悄悄断了没人知道"）

- **看门狗**：每 60s 检查（a）ffmpeg 进程活；(b) 当前分段文件尺寸在增长；(c) 最近 mtime < 120s。任一失败 → 指数退避重启该相机 + 写 `events.jsonl`（`stream_down` / `stream_recovered`）；
- **stats.json**（每分钟刷新）：每相机 {录制中, 当前分段, 今日字节, 磁盘剩余, 连续正常时长, 最近一次断流}；
- **启动自检**：`python -m capture.doctor`——逐相机试连 10s，报告编码参数（决定 copy 可行性）与建议 mode。

---

## 5. 交付计划（批准后执行）

| 阶段 | 内容 | 验收 |
|---|---|---|
| M0（半天） | capture 骨架：单相机 ffmpeg 拉流分段落盘 + 看门狗重启 | 一路真实流连续录 2h，断网重连后自动恢复，分段完整可播 |
| M1（半天） | 相机注册表多相机化 + 保留策略双闸 + stats/events | 删旧正确、磁盘水位触发正确、拔网线有事件记录 |
| M2（半天） | doctor 自检 + index.jsonl 清单 + vus 消费联调 | `vus.integrated_pipeline` 成功处理任一历史分段出报告 |
| M3（按需） | 事件分段录制（主码流触发式，为语义层准备）/ MQTT 状态出口 | — |

---

## 6. 与既有资产的关系

- **vus**：唯一上游。本层不读帧不分析，只生产"vus 可消费的文件"；vus 的 RTSP 源参数经验（重连/背压）直接复用其实现思路；
- **语义摄像头 v2 设计稿**：暂缓保留。本层即其 §10 开放问题 #1 的答案（录像凭据=本层产物），语义层未来作为本层的消费方挂接，设计稿届时小幅修订即可；
- **旧仓库**：继续封存不推送，与本层无关；
- **家庭网关**：作为一路源接入（HLS/WHEP 转投或直拉），不再是系统必需件。

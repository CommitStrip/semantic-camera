# 设计稿 v0.5 · 工作台界面 + 模型双通道接口

> 状态：**待审阅**
> 日期：2026-09-17
> 前置：v0.2 发现直连 / v0.3.1 首次环境识别与圈选 / v0.4 快慢分层
> 部署前提：Linux NVR 常驻，工作台 = NVR 内置 Web 服务，管理员用任意设备浏览器访问

---

## 0. 工作台的信息架构（六个视图）

| 视图 | 承载的业务 | 核心元素 |
|---|---|---|
| **① 值守**（默认首页） | 日常监控 | 多路播放网格、告警横幅、最新事件滚动条 |
| **② 首次环境识别**（全屏磨砂门） | 语义层第一步 | 磨砂覆盖 + 【开始识别】+ 识别结果（场景/要素/风险） |
| **③ 圈选编辑器** | 区域与规则 | 画面网格（22×18）点选 + **自定义规则模板库**（内置三模板仅为预设，可增删改：名称+结构条件[区域/类别/时间窗/滞留]+可选语义描述）+ 保存 |
| **④ 事件工作台** | 审查与检索 | 事件列表（筛选：相机/时间/类别/状态）+ 详情（短名/**细节描述**/rationale/关键帧/命中模板）+ 误报标记与改名（金标） |
| **⑤ 模式库** | 习惯化资产 | 模式列表（状态/计数/细节描述/嵌入档案）、审计抽检结果、管理员改名 |
| **⑥ 系统** | 健康与设置 | 相机健康、存储/保留、**模型接口（本地/云端）**、出口（webhook/MQTT）、**模板库管理** |

**导航原则**：值守页常驻；磨砂门只在未完成环境识别的相机上强制出现；其余视图从顶栏进入。**桌面优先**（手机适配暂缓不做）。

---

## 1. 后端 API（NVR 内置，工作台的数据面）

```
GET  /api/cameras                  相机列表与健康
GET  /api/events?camera=&type=&from=&to=   事件查询（分页）
GET  /api/events/{id}              事件详情（三层文本+关键帧+命中规则）
POST /api/events/{id}/feedback     金标反馈 {misreport:true|name:"..."}
GET  /api/patterns                 模式库列表（状态/计数/档案）
POST /api/patterns/{id}/rename     管理员改名
GET  /api/environment/{camera}     环境档案
POST /api/environment/analyze      触发环境识别（管理员按钮）
GET/POST /api/zones/{camera}       网格区域读写
GET  /api/health                   服务/相机/存储健康
POST /api/model-config             模型通道配置与连通性测试
SSE  /api/stream                   实时事件/告警推送
POST /api/zones/{camera}/frame     圈选底图（当前帧快照）
```

前端为 NVR 自带静态页（无构建链，vanilla JS + SSE），**装完即有界面，零前端工程**。

---

## 2. 模型双通道接口（本地模型 / 云端 API）

所有语义动作（环境识别、命名、细节描述、行为判定）经**统一 Provider 接口**，后台可切：

```json
"model": {
  "channel": "local",              // "local" | "cloud"
  "local":  { "type": "ollama", "model": "qwen3-vl:2b",
              "base": "http://127.0.0.1:11434" },
  "cloud":  { "type": "openai-compat", "base": "https://...", "model": "...",
              "key_ref": "env:CLOUD_VLM_KEY" }   // 密钥只走环境变量/密钥服务
}
```

| 通道 | 用途 | 特点 |
|---|---|---|
| **local（默认）** | ollama 本地 VLM + V-JEPA 本地 ONNX 嵌入 | 零成本、数据不出 NVR、离线可用 |
| **cloud（显式启用）** | 任意 OpenAI 兼容云端 VLM API | 能力上限更高；**帧/内容会出 NVR——启用时管理页强制警示确认** |

**路由与纪律（不因通道而变）**：
- 预算体系对两通道同样生效（地板间隔/单飞/按需递增帧）；
- 本地通道故障自动降级为模板占位名（绝不阻塞快路径——报警路径本就零模型）；
- 云端仅影响 T2b 深理解与环境识别；T2a 嵌入默认本地 V-JEPA（毫秒级），云端嵌入仅为可选；
- 密钥管理：环境变量或密钥服务注入，源码/配置示例/测试零凭据字面量；
- 连通性测试按钮：管理页一键测通（列出可用模型/延迟），配置错误当场可见。

## 3. 设计原则（工作台自身的三条纪律）

1. **不越俎代庖**：工作台只呈现状态、收集管理员动作；不自动改配置、不自动启用云端、不自动圈选；
2. **零前端工程**：静态页随 NVR 分发，升级=换文件；不引入 Node 构建链；
3. **桌面优先**：管理操作面向桌面浏览器设计（手机适配暂缓不做）。

## 4. 交付顺序

| 阶段 | 内容 |
|---|---|
| W1 | 后端 API 骨架 + 值守页（播放网格/告警横幅/事件流） |
| W2 | 首次环境识别磨砂门 + 环境档案展示 |
| W3 | 圈选编辑器 + 事件工作台（金标反馈） |
| W4 | 模型双通道配置页 + 连通性测试 + 模式库视图 |

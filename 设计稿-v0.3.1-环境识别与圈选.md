# 设计稿 v0.3.1 · 首次环境识别（体验定义稿）

> 状态：**待审阅**（本稿只定义体验与范围；VLM 输出格式、执行器选型、交付计划等实现细节**不在本稿**——到开发阶段另行确认）
> 日期：2026-09-17
> 前置：v0.2 自动发现直连（摄像头自动连接已完成）

---

## 0. 原则

1. **不越俎代庖**：不到时机不设计、不实现；系统一切语义动作由管理员动作触发。
2. **先识别后观看**：环境识别是使用监控的前置门——完成识别前，监控界面处于磨砂覆盖状态。

---

## 1. 体验定义

自动连接摄像头后，进入**全屏监控播放界面**——此时画面被**全屏磨砂层覆盖**（视频在磨砂后仅模糊可见，无法正常观看）。

磨砂层之上是环境识别引导：

- 说明文字：环境识别为首次使用必做步骤——建立该监控的场景档案，之后的所有视频理解基于此档案（更准、更省）；
- 【开始识别】按钮：管理员点击 → 系统执行环境识别（理解摄像头所能监控的画面）→ 识别结果展示；
- 识别完成 → 磨砂层解除 → 监控可正常观看，并进入圈选环节。

**不看识别不解除：磨砂覆盖是硬性前置，不是可关闭的提示。**

---

## 2. 本步范围（三个组件）

1. **环境识别**：抓监控画面 → VLM 理解（场景类型/要素/照明/建议区域/风险提示），存为该相机的环境档案；档案作为后续视频理解的持久上下文（省 token、提精度）；
2. **圈选编辑器（海康式网格圈选）**：整个画面被小正方形划分为网格（默认 22×18，对齐海康习惯，密度可调），**点击选中小方格即为重点管理区域**，拖拽刷选批量标记；选中格高亮，再点取消；
3. **报警执行器**：识别结果与**管理员自定义模板**比对，符合即报警；未命中任何模板不报警——一切报警源于管理员定义的模板。

实现细节（识别输出格式、检测器方案、界面技术选型、交付排期）**均不在本稿**，进入开发阶段前逐项与所有者确认。

---

## 附一：模板机制（用户自定义，报警=识别对比模板）

- 模板 = 管理员自定义的"什么情况要报警"，两部分组成：
  - **结构条件**（实时层，≤1s 报警）：网格区域（复用圈选格子）+ 类别 + 时间窗 + 滞留秒数——快系统识别结果实时比对，符合即报警；
  - **语义描述**（可选增强）：自然语言细节描述（"有人翻墙进入而不是走大门"）——慢系统识别的细节描述与之语义比对，符合即补充报警（比结构条件慢，但能抓结构条件表达不了的情形）；
- 内置三模板（进入滞留/徘徊/即时出现）仅作**预设示例**，管理员可增删改，模板库随场所档案持久化。

---

## 附二：网格圈选的技术映射（实现期参考，非本稿承诺）

- **数据**：区域 = 选中格子的索引集合（如 `cells: [37,38,59,60,...]`），随相机持久化；密度可配置；
- **判定**：检测框中心点 → 算出所在格子（归一化坐标 × 网格数取整）→ 格子被选中即在重点区域内（无需点在多边形内计算）；
- **交互**：单击切换单格；按住拖拽连续刷选；支持"全选/清空"按钮。

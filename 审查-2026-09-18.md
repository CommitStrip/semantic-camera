# 审查报告 · 2026-09-18

> 性质:独立只读代码审查 + 复测快照,范围 = 当日 7 个提交(cf8ea6b→85b7b81,均已在 origin/main)。
> 修复完成、逐条核销后本文件可删除。审查会话未改动任何仓库文件。

## 复测记录

| 项目 | 结果 |
|---|---|
| `python -m pytest tests/ -q`(Python 3.12.10) | **35 passed**,与提交信息一致 |
| `python -m scam.nvr`(临时 cameras.json,无相机) | **NameError: name 'threading' is not defined**(nvr.py:151,入口即崩) |
| `scam.nvr` 模块命名空间探测 | `time`/`threading` 均不存在 |
| Monitor 合成目标滞留区域 5 秒(50 步) | **46 条告警,46 个不同 event_id**(同一事件应 1 条) |

测试套件全绿但未覆盖 `nvr.py` 生产入口与告警冷却路径——35 绿与"入口必崩"并存,是本轮最重要的复测结论。

## P0(入口/核心功能不可用,建议先修)

### 1. nvr.py 缺 `import time` 与 `import threading`;且在子线程调用 signal
- 位置:`scam/nvr.py:7-10`(导入区)、`:78,108,117,132`(time)、`:151`(threading)、`:68-69`(signal.signal)
- 提交:7cf90b8
- 复测:见上表,`python -m scam.nvr` 直接 NameError。即使补上导入,`main()` 把 `nvr_main` 放进 `threading.Thread` 运行,其中 `signal.signal` 在非主线程抛 ValueError。`deploy/install.sh:66` 与 `deploy/scam-nvr.service:9` 注册的正是此入口,systemd Restart=always 将无限崩溃循环。
- 方向:补两行导入;信号改为主线程统一注册(`main()` 内),或线程内用 `Event` 停机。

### 2. nvr 主循环每帧重建 MotionGate,`last_det` 永不更新——T0 门控与检测节流全部失效
- 位置:`scam/nvr.py:82`(循环体内 `gate = MotionGate()`)、`:62`(`last_det=None` 后循环内无赋值)、`:86`
- 提交:7cf90b8
- 影响:每帧都是新门控=永远在首帧预热,motion 恒 False;`_patrol_due(None)` 恒 True → 每帧全帧 NanoDet 推理(≈25fps),400ms/5s 节流形同虚设。`monitor.py:52-63` 是正确参照。
- 方向:检测节流与告警判定直接复用 `Monitor.step`,删除 nvr_main 内联重写(见问题 7)。

### 3. 工作台无启动入口;`/api/zones/save` 假成功;多区域 zone 归属错标——部署文档的首次使用流程走不通
- 位置:`scam/server.py:125-129`(`WorkbenchServer` 全仓零调用,8600 端口无人监听)、`:119-121`(save 返回 `{ok:true}` 但无持久化)、`scam/monitor.py:41-44` 与 `scam/nvr.py:52-55`(cells 取并集、rules 拼接、`zone_id` 只剩最后一个 zone)
- 提交:14c15df / 95f939f
- 影响:deploy/README.md:34-39 的"打开工作台→圈选→保存→值守"每一步都断;多区域配置下所有告警 zone 字段错标,滞留计时也变成合并域。
- 方向:nvr 启动线程内拉起 WorkbenchServer;save 落盘 venue.json 或如实返回 501;zones 逐区域独立判定与计时。

## P1(数据正确性 / 安全 / 验收可信度)

### 4. 告警风暴:规则命中后无冷却去重,event_id 毫秒时间戳使 `INSERT OR IGNORE` 失效
- 位置:`scam/monitor.py:74-80`、`scam/nvr.py:98-115`(`event_id=f"...{int(ts*1000)}"`);测试 `tests/test_monitor.py:67` 仅断言 `>=1`
- 提交:95f939f / 7cf90b8
- 复测:滞留 5 秒 46 条告警、46 个不同 id。目标站 1 分钟 ≈1500 条入库+横幅刷屏。
- 方向:`(track_id, rule)` 维度加告警冷却(如触发后静默 N 秒或离区重置);event_id 改用不含时间的稳定键或入库前查重。

### 5. 工作台 0.0.0.0 无鉴权,`/api/cameras` 原样返回含 RTSP 凭证的完整配置
- 位置:`scam/server.py:126`(默认 host)、`:81-83` + `:42-47`(整份 cameras 数组直出,source 含 `rtsp://user:pass@`)
- 提交:14c15df
- 方向:默认绑 127.0.0.1,远程访问交反向代理;`/api/cameras` 出口剥离 source 凭证字段。

### 6. acceptance.py 验收门度量错误 + 模型路径不一致 + 异常全吞
- 位置:`scripts/acceptance.py:92`(`lat = time.time() - t0`,量的是脚本启动至今,非检测→告警延迟)、`:74-77`(NanoDet 在检测循环内逐次重建 ONNX session)、`:75`(默认路径 `person-detector.onnx` 与 install.sh 生成配置的 `models/person-detector.onnx` 不符)、`:80-81`(`except Exception: pass`)
- 提交:7cf90b8
- 影响:模型加载失败被静默吞掉→"告警延迟: N/A"假阴性;"快 ≤1s"验收线度量对象错误。
- 方向:延迟改为"检测帧时间戳→告警时间差";检测器实例化移出循环;加载失败计数并在报告 FAIL。

### 7. 接线缺失:慢系统与出口零调用,README 描述与仓库不符
- 证据(grep 全仓):`Segmenter`/`NamingLanes`/`build_sinks` 除定义与测试外零引用;`nvr.py:1-4` 声称"起工作台 HTTP 服务"但无 import server;`README-CN.md:58` 列出 `outbox.py`(文件不存在)、`:55` 称 server 有 SSE(实为 3 秒轮询)
- 提交:1511103 / 14c15df / 85b7b81
- 影响:P2 慢系统(35 测试覆盖)在生产入口零调用;新用户按 README 快速开始必失败。
- 方向:短期先改 README 与现实对齐;中期把 Segmenter→NamingLanes→snapshot 持久化接入段关闭事件。

### 8. .mimosa 会话状态文件已在公开历史
- 事实:cf8ea6b 新增 3 个 `.mimosa/hook-status/sess_*.json`,1511103 删除;origin/main=85b7b81 已推送。内容为 sessionId/时间戳/文件名,低敏感;`.gitignore` 现有 `.mimosa/`(第 8、9 行重复两行,顺手去重)。
- 是否改写公开历史清洗,待决策。

## 中置信(条件成立,列此备查)

- **vus 硬依赖与安装脚本矛盾**:`scam/source.py:24` `from vus.source import ...`(惰性),`nvr_main` 无条件构造 `CameraSource`——新机未装 vus 时入口 ImportError;而 install.sh:29 输出"快系统不依赖 vus"。本机 vus 可导入故未复现,代码路径明确。方向:补 source 的本地回退(cv2.VideoCapture)或 install.sh 默认装 vus。
- **models.py**:本地通道构造参数 `num_ctx/num_predict` 被 understand() 内硬编码 16384/2048 覆盖(`scam/models.py:104-109` vs `:93-100`),若为绕 think 模型缺陷的刻意硬保证,建议删掉构造参数或注释说明;云通道只查字面 IP 不解析 DNS(`:131-132`),与文件头声称的"解析后 IP 阻断"不符;本地通道校验与请求间存在 DNS 重绑定窗口(`:57-74` 校验、`:32` 请求)。
- **config 校验器类型不设防**:`config.py:58` grid 非 dict 时 AttributeError、`:65` zones 非列表时同理——校验器应返回错误列表而非崩溃;且 `nvr.py:141-147` 生产入口根本未调用 `validate_venue`(导入未用)。
- **nvr dwell 语义分叉**:`nvr.py:100,111` 用 `ts-bornAt` 当滞留(区域外游荡时间也计入 enter-dwell);`nvr.py:58-59` 的 `ZoneRuntime`/`sinks` 实例化后未用。
- **ZoneRuntime 慢泄漏**:`monitor.py:71` 只对活轨迹调 update,轨迹在区域内老化消亡后 `(track_id, zone)` 条目无 `leave()` 清理,track id 单调递增→长期值守缓慢累积。
- **部署模板引用不存在的模型**:install.sh:41 写入 `models/person-detector.onnx`,仓内无该文件亦无下载步骤(`.gitignore` 排除 `*.onnx`),叠加问题 6 的异常吞,装完静默零检测。
- **跨端(条件性)**:告警横幅含 emoji(`sinks.py:14`、`nvr.py:59,114`、`discover.py:162`),Windows 控制台 GBK 且输出重定向时 UnicodeEncodeError;deploy 链为 Linux-only,README-CN 快速开始在 Windows 不可走通。
- **discover.py**:`:163` 打印含凭证 URL、`:104-106` ffprobe argv 使密码暴露于进程列表;密码含 `@`/`:` 未转义会构造坏 URL;254 主机×2 端口串行扫描最坏 ~8.5 分钟。

## 建议修复顺序

1(入口)→ 2(门控/节流)→ 4(告警冷却)→ 3(工作台/save/zones)→ 6(验收脚本)→ 5(凭证出口)→ 7(README 对齐)→ 8(历史清洗,待决策)。
1+2+4 修完才具备 P5 真机浸泡的前置条件;修完可跑 `python -m scam.nvr` 冒烟 + 以滞留 5s 告警 ≤1 条为回归断言补测试。

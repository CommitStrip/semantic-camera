# 设计稿 v0.1 · 极简接入（已被 v0.2 部分取代：前置新增自动发现直连，见 设计稿-v0.2-自动发现直连.md；启动器/守护部分仍以本稿为准）

> 状态：**待审阅**（本稿取代：设计稿-v2-重铸.md【暂缓】与 设计稿-基础层-监控流本地化.md【废弃】）
> 日期：2026-09-17
> 原则：**安装零特殊工作、流接口直连业务、不存本地**

---

## 0. 结论先行

**不需要新建项目。** 用户要的"把监控视频流接口接到业务里"，vus.live 今天就能做：

```bash
git clone https://github.com/CommitStrip/video-understanding-skill
cd video-understanding-skill && pip install -e .     # 唯一安装步骤，模型自动下载
python -m vus.live --source rtsp --url "rtsp://user:pass@相机/流" --serve
```

业务方从这三处拿理解结果（均为 vus.live 原生产出）：
- `live_state.json` / `live_context.md`（滚动落盘，任何程序随时读）
- SSE 服务 `GET /state` `/events` `/healthz`（实时推送）
- 控制台状态行

VLM 增强可选：本机 `ollama serve` + 环境变量（VLM_API_BASE 指向 ollama 的 OpenAI 兼容端点）即启用 T2 理解；不开则纯本地零成本运行。

## 1. 唯一值得补的代码：多路启动器（~150 行）

vus.live 一次跑一路。多路监控需要一个小启动器 `scam`：

- 读 `cameras.json`（每路：name + stream URL + 开关）；
- 每路起一个 `vus.live` 子进程，崩溃/断流自动重启（7×24 唯一必需的健壮性）；
- 产出目录按相机名分目录。

除此之外**零新代码**。语义能力（场所规则/命名/习惯化/嵌入）未来的归宿是**作为功能上游进 vus.live**（理解管线的增强项），不再是独立系统——这从根本上消除了"独立项目需要独立安装"的问题。

## 2. 明确放弃的（及代价）

| 放弃 | 代价 | 接受理由 |
|---|---|---|
| 本地录像/保留策略 | 断流期间画面永久缺失，无历史回溯 | 业务是**实时理解**而非取证回溯；未来要回溯再加回（设计已存档） |
| 浏览器端推理 | "浏览器里算"的叙事 | 换成"vus 单命令"，安装复杂度归零 |
| 桥/网关/独立配置系统 | 多相机管理略糙 | 三协议边界与全部接线层 bug 根除 |

## 3. 路线

| 阶段 | 内容 | 工作量 |
|---|---|---|
| v0.1 | `scam` 启动器 + cameras.json + 进程守护 | ~150 行 + 半天 |
| v0.2 | 场所规则进 vus.live（作为上游功能：区域/时间表过滤理解触发） | 与 vus 主线协调 |
| v0.3 | 语义命名/习惯化上游化（替代模板时间线） | 按需 |

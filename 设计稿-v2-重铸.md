# 语义摄像头 v2 · 设计稿（代号：重铸）

> 状态：**待审阅**（审阅人：项目所有者；本文为设计稿，未获批准前不动一行实现代码）
> 日期：2026-09-17
> 上游：**仅 VUS**（video-understanding-skill）。其余一切历史仓库（anti-drone-monitor 等）不再是叙事的一部分。
> 仓库策略：**冻结推送**。新工作在 `视觉未来/语义摄像头/` 本地演进，逻辑诟病清完之前不上传。

---

## 0. 为什么重来（病灶诊断）

### 0.1 三代遗留的堆积史

| 代 | 载体 | 留给现在的东西 |
|---|---|---|
| 一代 | anti-drone-monitor | 浏览器做检测的技术路线、"选模式=选能力"的闭集叙事、仓库里的凭证与旧路线历史 |
| 二代 | semantic-camera（v1/v2 机制层） | 把 vus 的感知思想在浏览器里**手搓了一遍劣化版**（门控/关键帧/检测全用 JS 重写），外加三进程三协议 |
| 三代 | v3 循环层 | 正确的设计（分段/签名/习惯化/四态/ICR/环境模型），但寄生在二代的地基上 |

### 0.2 逻辑诟病清单（本轮要解决的"诟病"，全部是结构性问题）

1. **感知层重复造轮子**：vus 已有 FrameSource（文件/相机/RTSP）、运动门控、关键帧、预算制 VLM 调用、流式状态服务；semantic-camera 却在浏览器里用 wasm/JS 重实现了一遍低配版，且每一块都更脆。
2. **浏览器承载推理 = 一切工程病的温床**：两轮 P0 崩溃链（TDZ/导出缺失）全部出在内联接线层；data URL 前缀缺陷；IndexedDB 手搓持久化；wasm 多线程要 COOP/COEP；**浏览器层永远无法做执行级自动化测试**（QA 用无头浏览器才抓到）。
3. **一条视频流穿四个进程边界**：RTSP → MediaMTX →（WHEP/HLS）→ 浏览器 →（WS）→ 桥 →（HTTP）→ ollama。每条边界都是一类 bug 的栖息地（data URL、协议不匹配、超时语义各自为政）。
4. **双配置系统**：模式包与环境模型语义重叠（场所语义/相机工程两层纠缠不清），admin 心智负担大。
5. **没有录像与审查凭据**：告警只有语义记录，复核时"口说无凭"。
6. **仓库历史污染循环**：凭证/旧路线在 git 历史里，导致"清理—复发—再清理"的死循环（drone.p12、网关密码）。

### 0.3 刮骨疗毒的三条决定（已定，本设计稿在其框架内工作）

1. **停止推送**：旧仓库冻结为只读封存，不再上传；历史问题（密码轮换/历史清洗）与新项目解耦，由所有者另行处置。
2. **唯一上游 = VUS**：新项目的感知、预算、流服务全部建立在 vus 之上；与 anti-drone 一脉的技术叙事（浏览器检测）彻底切割。
3. **先逻辑后实现**：本设计稿解决"逻辑诟病"，获批准后才开第一行代码。

---

## 1. VUS 逻辑解读（唯一上游的真实能力清单）

基于 vus/live 实码（pipeline.py / events.py / server.py / vlm_client.py / state.py）：

### 1.1 四层实时栈（run_live，阻塞式主流程）

| 层 | 现状 | 语义摄像头视角 |
|---|---|---|
| T0 快系统 | SmartPipeline：帧差门控、运动事件、关键帧事件（帧在循环体内，keyframe 直接 `cv2.imwrite` 落盘） | **直接复用**，不再重写 |
| T0.5 毫秒标签 | labeler 即席标签进总线（`{"type":"tag", t, labels, ms}`） | 直接复用（标签可作语义特征） |
| Tier2 关键帧 | 关键帧 JPEG 落盘 + 事件带路径 | 直接复用（嵌入/命名的图像来源） |
| T2 预算制理解 | UnderstandingWorker：触发式 VLM + min_call_interval 成本地板 + 单飞合并 + 滚动时间线 | 直接复用（命名/裁决挂进同一预算体系） |

### 1.2 基建件（全部现成）

- **FrameSource**：文件/相机/RTSP 统一接口（最新帧背压、断线重连、单调时钟）——浏览器+MediaMTX 网关的替代品；
- **EventBus**：线程安全有界扇出（publish 永不阻塞、慢订阅者丢最旧）——语义层的标准接入点；
- **LiveServer**：SSE `/state` `/events` `/healthz`——语义状态的现成出口；
- **StateWriter**：滚动状态文件（live_state.json / live_context.md）——agent 可读接口的现成范式；
- **vlm_client**：VLM 后端注册（openai 兼容/mock）——ollama 命名的调用底座。

### 1.3 总线上流动的事件（实证）

`motion`（含 motion_ratio）、`keyframe`（含落盘路径）、`tag`、`asr_final`、`understanding`。语义层需要的**原始帧**目前只在帧循环体内（keyframe 落盘处）——这是唯一的接入缺口，见 §4.9。

### 1.4 VUS 明确没有的（= 语义摄像头的存在空间）

场所/区域/时间表概念、危险行为定义（开放集 admin 主笔）、四态裁决、事件分段与行为签名、模式习惯化（成本摊销）、证据事件与出口预算、ICR 成像模态感知、多相机场所组织。**这些是语义摄像头 v2 的全部新增量，一个不多一个不少。**

---

## 2. 新定位

> **语义摄像头 v2 = vus.live 之上的场所语义值守层（纯 Python 单进程应用）。**

一句话分工：**vus 负责"看懂画面"（感知、压缩、预算制理解），语义摄像头负责"这个场所什么算事、怎么裁决、如何记账与摊销"。**

命名建议随架构一起换血："语义摄像头"对内保留为项目名，对外表述对齐业界口径——**semantic video event runtime（预算受控的语义视频事件运行时）**。

---

## 3. 架构总览

```
RTSP/文件/相机
   │ FrameSource（vus）
   ▼
vus.live  run_live（T0 门控 → 关键帧 → T0.5 标签 → T2 预算制理解）
   │        ▲ frame_hook（§4.9，唯一的上游增量）
   ▼        │
EventBus 订阅 + 帧钩子
   ▼
┌────────────────── 语义层（新写/移植，纯 Python 单进程）──────────────────┐
│ 检测(可选 onnxruntime) → 跟踪确认 → 时空规则(zones/lines/schedule)        │
│ → 四态裁决 → 事件分段+行为签名 → 命名/行为判定(进程内 ollama，预算制)     │
│ → V-JEPA 段嵌入 → PatternLibrary 习惯化 → 证据事件 → Outbox(webhook/MQTT)│
│ ICR 成像模态状态机（跨层横切：剖面切换/学习冻结）                        │
└──────────────────────────────────────────────────────────────────────┘
   ▼ SQLite（events/segments/patterns/embeddings）   ▼ SSE + 只读仪表盘
```

**删除的东西**：浏览器推理层（web/ 全部 wasm/JS 感知与 IndexedDB）、桥 WS 协议（bridge/ 的服务端与客户端——VLM 改进程内调用）、MediaMTX 网关依赖（vus RTSP 源原生直连）、data URL、三协议边界。

---

## 4. 模块设计（职责 / 来源 / 要点）

来源标记：**[vus]** = 直接用上游；**[移植]** = 现 JS 已验证逻辑平移 Python（设计不变、代码重写）；**[新]** = 新增。

### 4.1 感知与检测 [vus + 移植]
- 门控/关键帧/标签：[vus] SmartPipeline 原样；
- 对象检测（可选增强）：NanoDet ONNX 移植为 Python onnxruntime 推理（GPU 可用，23ms/帧 CPU 实测）；v3.6 的"检测可选"原则保留——`detector: none` 时纯 vus 感知照常值守；
- 触发式节奏保留（有运动 400ms / 巡检 5s）。

### 4.2 跟踪与确认 [移植]
恒速预测 + 分级老化 + 中心距硬闸门 + 异类不改写身份（QA 修复语义随逻辑平移）。

### 4.3 时空规则 [移植]
zones（进区+滞留+计数）、lines（方向越线）、schedule（布防表，跨零点）。数据格式不变。

### 4.4 四态裁决 [移植]
alert/escalate/clear/suppress + via 标记；fail-open（告警）/fail-closed（配置）分界不变。

### 4.5 事件分段 + 行为签名 [移植]
20s 静默 / 120s 上限、签名七分量 + dwellMaxMs（QA 修复保留）、0.82 阈值。

### 4.6 命名与行为判定 [新壳 + vus 底座]
- 进程内调用 ollama（复用 vus.live VLM 后端注册模式），**桥 WS 协议删除**；
- 命名预算 30/时 + 重要性门控 + 模板占位名降级——预算语义对齐 vus.live 的 min_call_interval 体系；
- 输出规范不变（≤16 字中文名 + conf + matchedBehaviors + undecidable 诚实选项 + rationale）；
- 思考模型硬保障（num_ctx/num_predict）等实测经验随迁。

### 4.7 V-JEPA 段嵌入 + 习惯化 [移植]
- 导出图与预处理 parity 结论（292 BILINEAR→裁 256²→ImageNet 归一化、自然帧序、all-token mean）平移为进程内函数——**data URL 问题在根上消失**（帧直接是 ndarray）；
- PatternLibrary：信任分层（draft→model-verified→human-verified）、审计抽检 5%、预期窗口+偏离检测、维度守卫——全部移植；
- **联合匹配**（同模态 0.7 结构 + 0.3 嵌入）保留。

### 4.8 证据事件 + 出口 [移植 + 新]
- sc.evidence/v1 语义不变（稳定 ID/双时间戳/mode_hash/前因帧环）；
- Outbox at-least-once + 死信保留；出口一期 webhook、二期 MQTT（Frigate 生态印证）。

### 4.9 vus 上游增量（唯一要动 vus 的地方）[vus + 极小改动]
语义层需要**每帧/每检测时机拿到帧**，当前总线事件不带帧。三个选项：

| 选项 | 做法 | 代价 |
|---|---|---|
| **B（推荐）** | vus.live `run_live` 增加 `frame_hook(callable)` 参数：帧循环内每帧/关键帧处调用，默认 None 零影响 | vus 上游 +3 行；可提回 vus 主线成为正式扩展点 |
| A | 语义层只订阅总线，用关键帧落盘图做检测/嵌入 | 零上游改动；但检测失去逐帧时机，语义密度降级 |
| C | 装饰器包 SmartPipeline/FrameSource | 零上游改动；但与 run_live 内部耦合，升级脆弱 |

### 4.10 ICR 成像模态状态机 [移植]
多信号表决/锚定参考/振荡态/剖面切换——跨层横切（门控软重置、学习冻结钩子语义保留）。

### 4.11 状态服务与 UI [vus + 新薄壳]
- SSE/状态文件：[vus] LiveServer + StateWriter 原样；
- UI：**只读仪表盘 + 审查列表**（看状态/看事件/确认误报），浏览器零推理——Frigate 的"预览网格时间线"作为审查 UI 的交互范本。

### 4.12 存储 [新]
SQLite 单文件：events（30d）/ segments（30d）/ patterns（永久，上限）/ embeddings。替代 IndexedDB。

---

## 5. 配置收敛：唯一产物"场所档案"（venue profile）

合并模式包与环境模型为一个 JSON/YAML，两个子对象各管一层（消除双系统）：

```yaml
venue:            # 场所语义层（多相机共享）：定位文本/要素/危险行为定义/照明基线
  behaviors: [...]
camera:           # 相机工程层（单相机视角）：检测绑定/区域/警戒线/布防表/预算
  detector: {engine: onnx|none, model: ..., conf: ...}
  zones: [...]
  schedule: [...]
  arb: {budget_per_hour: 20}
```

一个加载器、一个校验器（fail-closed 语义不变）、场景自识别降级为"模板推荐"（不再有"选模式=选能力"的开关语义）。

---

## 6. 数据流（一次夜间事件的完整旅程）

1. 22:00 布防表生效（schedule）；
2. T0 门控感知活动 → 触发检测 → person 确认 → 进入区域滞留 2s → zone-intrusion；
3. 四态裁决 alert → 证据事件（前因帧环+裁剪帧）→ Outbox → webhook；
4. 段关闭 → 签名 → 查模式库：命中 trusted 直接自命名（零 LLM）；未命中 → 预算内送 ollama 命名（≤30s）→ 修订链 r1→r2；
5. V-JEPA 段嵌入 → 模式档案（昼/夜双档案）→ 下一段联合匹配；
6. 全程 SQLite 可审计，SSE 实时可看，ICR 夜间切换时剖面自动跟随。

---

## 7. 删除与封存清单

| 对象 | 处置 |
|---|---|
| web/ 浏览器推理与 IndexedDB | 删除（逻辑移植 Python 后） |
| bridge/ WS 服务与协议 | 删除（进程内调用替代；ollama 经验随迁） |
| gateway/ MediaMTX | 删除（vus RTSP 源原生直连；家庭网关硬件留作它用） |
| 旧仓库 CommitStrip/semantic-camera | 只读封存不再推送；历史清洗（密码/旧路线）与本项目解耦，所有者另行处置 |

---

## 8. 迁移对照表（已验证逻辑的去向）

| 现 JS 资产 | 去向 | 备注 |
|---|---|---|
| core.js：Tracker/规则/裁决/分段/签名/PatternLibrary/ICR/Outbox | 逐模块移植 pytest | 109 个 JS 测试的设计断言随迁为 pytest 用例（逻辑等价，代码重写） |
| core.js：数据 URL/IndexedDB/浏览器 DOM | 丢弃 | 平台税，根上消失 |
| bridge/arbiters.py：OllamaArbiter/JepaEmbedder | 平移为进程内模块 | parity/预处理结论（292 BILINEAR/自然帧序/all-token mean）全部随迁 |
| 质量基建：TDZ 护栏 | 被 pytest + 常规 Python 测试替代 | 浏览器层消失=此类缺陷根除 |
| qa 清单未结项（TODO-11 传输加固等） | 转入新架构对应项 | 清单随迁 |

---

## 9. 风险与代价（诚实账）

1. **重写成本**：JS→Python 移植约等于把 109 个测试的设计断言重写一遍；核心纯逻辑（裁决/签名/习惯化）移植机械，接线层（占 bug 大头）反而是净删除；
2. **性能账**：vus 帧循环 1.6ms/帧 + 触发式 NanoDet 23ms（CPU）——预算内；GPU 就绪时更宽裕；嵌入 ViT-L CPU 数秒/段，段频 30/时可承受；
3. **隐私叙事变化**：从"浏览器端侧"变为"本机端侧"——帧不出设备的不变量不变，但不再是"浏览器里算"；
4. **单进程单点**：进程崩=值守断（缓解：systemd/看门狗 + /healthz 已有）；
5. **不确定项**：frame_hook 上游增量需要与 vus 主线协调（+3 行，风险极低）。

---

## 10. 待所有者裁决的开放问题

1. **录像与回放**：要不要保留短时录像作为审查凭据（Frigate 借鉴项）？隐私口径是否接受"告警前后 ±N 秒环形缓冲，超期即删"？
2. **UI 形态**：SSE + 静态只读页（最薄）够不够，还是要一个带审查操作的最小界面？
3. **检测器起步档位**：先 `none`（纯 vus 感知，最快跑通）还是直接上 NanoDet？
4. **vus 上游 frame_hook**：接受改 vus +3 行（推荐 B），还是强制零上游改动（A，接受语义密度降级）？
5. **命名口径**：对外名是否随架构定为"semantic video event runtime"？
6. **旧仓库处置细节**：封存即可，还是同时启动密码轮换+历史清洗（与本解耦，可并行）？

---

## 11. 批准后的路线图（草案）

| 阶段 | 内容 | 验收 |
|---|---|---|
| M0 | 语义层骨架：EventBus 订阅 + frame_hook + 场所档案加载器 | 测试视频跑通"活动→段→签名→SQLite" |
| M1 | 规则+四态+证据事件+Outbox | 夜间场景告警端到端 |
| M2 | 命名/裁决（进程内 ollama）+ 预算 | 真实段命名 ≤30s，预算封顶 |
| M3 | 嵌入 + PatternLibrary 习惯化 | 同签名第二段零 LLM 命中 |
| M4 | ICR + SSE/UI 薄壳 + 保留策略 | 一周连续值守内存平稳 |
| M5 | 多相机 + 场所共享（远期） | — |
